<!DOCTYPE html>
<html>
<head>
  <title>GnarPow</title>
  <link href='https://fonts.googleapis.com/css?family=Permanent+Marker|Covered+By+Your+Grace|Walter+Turncoat|Tillana|Gloria+Hallelujah' rel='stylesheet' type='text/css'>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.2/css/materialize.min.css">
  <link rel="stylesheet" type="text/css" href="css/jquery.countdown.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="Site">
  <% if current_admin? %>
    <%= render "partials/admin_nav" %>
  <% else %>
    <%= render "partials/nav" %>
  <% end %>
  <main>
    <% if current_page?("/") %>
      <%= yield %>
    <% else %>
    <div class="container">
        <% flash.each do |type, msg| %>
          <%= content_tag :div, sanitize(msg), class: "flash_#{type} hidden" %>
            <body onload="Materialize.toast('<%=msg%>', 1500)">
        <% end %>
    <%= yield %>
  </div>
  <% end %>
  <script type="text/javascript" src="js/jquery.plugin.js"></script>
  <script type="text/javascript" src="js/jquery.countdown.js"></script>
   <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.2/js/materialize.min.js"></script>
 </main>
 <%= render "partials/footer" %>
</body>
</html>
